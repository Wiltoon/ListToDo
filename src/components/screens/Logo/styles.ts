import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    iconlogo: {
        width: 30,
        height: 30,
    },
    to: {
        fontSize: 48,
        color: '#4EA8DE',
        fontWeight: 'bold',
        marginTop: 48,
      },
    do: {
        fontSize: 48,
        color: '#5E60CE',
        fontWeight: 'bold',
        marginTop: 48,
        marginBottom: -350
    },
});